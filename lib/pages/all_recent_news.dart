import 'package:flutter/material.dart';

class AllRecentNews extends StatelessWidget {
  const AllRecentNews({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('All recent news'),
    );
  }
}